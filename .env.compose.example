# Docker Compose Infrastructure Configuration
# Copy to .env.compose and fill in values
#
# Purpose: Container-level config only (ports, credentials, volumes)
# Used by: docker-compose.yml
# NOT used by: Application code directly

# Postgres Container
POSTGRES_USERNAME=personal_finances
POSTGRES_PASSWORD=changeme
POSTGRES_DATABASE=personal_finances

# Port Configuration
POSTGRES_PORT=5432
DAGSTER_WEBSERVER_PORT=3001
FRONTEND_PORT=3000
API_PORT=8000
