# Frontend Defaults
#
# Copy to .env and modify as needed:
#   cp .env.example .env
#
# Note: Only NUXT_PUBLIC_* variables are exposed to the browser

# Environment
NUXT_ENV=local

# API URL (where the browser will call the backend)
# - Local dev: http://localhost:8000
# - Docker: Set via docker-compose.yml environment override
# - Production: https://api.yourdomain.com (via Cloudflare tunnel, etc.)
NUXT_PUBLIC_API_URL=http://localhost:8000
