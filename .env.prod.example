# Production Database Connection
# Copy to .env.prod and fill in values
#
# Used by: make clone-prod

# Required: your prod server hostname/IP and password
PROD_POSTGRES_HOSTNAME=your-prod-host
PROD_POSTGRES_PASSWORD=your-prod-password

# Optional: override if different from local defaults
# PROD_POSTGRES_PORT=5432
# PROD_POSTGRES_USERNAME=personal_finances
# PROD_POSTGRES_DATABASE=personal_finances
